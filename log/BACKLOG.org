#+TITLE: Product Backlog
#+STARTUP: overview

All planned features, sorted by category.

* Product Backlog

** DONE Tree-sitter (treesit)
   :PROPERTIES:
   :Effort:   3
   :Category: :editor:
   :Sprint:   5
   :END:
   Better syntax highlighting via treesit (built-in Emacs 30).

** DONE Desktop Save
   :PROPERTIES:
   :Effort:   1
   :Category: :editor:
   :Sprint:   5
   :END:
   Session persistence (files, buffers, windows).

** DONE Consider Tempel
   :PROPERTIES:
   :Effort:   1
   :Category: :editor:
   :Sprint:   6
   :END:
   Research: evaluate Tempel as modern snippet alternative to Tempo.

** DONE Git pre-commit hook
   :PROPERTIES:
   :Effort:   2
   :Category: :devops:
   :Sprint:   5
   :END:
   Automatic testing before each commit.

** DONE Docker (docker.el)
   :PROPERTIES:
   :Effort:   2
   :Category: :devops:
   :Sprint:   5
   :END:
   Docker container management (dockerfile-mode already added).

** DONE Workflow Tutorials & Tests
   :PROPERTIES:
   :Effort:   5
   :Category: :docs:
   :Sprint:   7
   :END:
   Create tutorials for each workflow (lsp, git, build, debug) with test cases.

** DONE Coverage report
   :PROPERTIES:
   :Effort:   2
   :Category: :testing:
   :Sprint:   6
   :END:
   Track test coverage.

** DONE Testing Strategy (modules, integration, usability)
   :PROPERTIES:
   :Effort:   3
   :Category: :testing:
   :Sprint:   7
   :END:
   Three-tier testing: module tests (unit), integration tests (workflow), usability tests (human-in-the-loop).

** DONE CHANGELOG.md
   :PROPERTIES:
   :Effort:   1
   :Category: :docs:
   :Sprint:   6
   :END:
   Not needed — SESSION.org provides detailed changelog. Git log for commit history.

** DONE Org-babel (all configured languages)
   :PROPERTIES:
   :Effort:   3
   :Category: :lang:
   :Sprint:   8
   :END:
   Enable org-babel for Python, C/C++, Java, Shell, Emacs Lisp.
   - Configure org-babel in init.el
   - Enable Python code block execution
   - Enable C/C++ code block execution
   - Enable Java code block execution
   - Enable Shell code block execution
   - Enable Emacs Lisp code block execution
   - Add org-mode settings (structure, exports)
   - Add test for org-babel Python
   - Add test for org-babel C/C++
   - Add test for org-babel Java
   - Add test for org-babel Shell
   - Add test for org-babel Emacs Lisp

** TODO Org-mode LaTeX support
   :PROPERTIES:
   :Effort:   2
   :Category: :lang:
   :Sprint:   9
   :END:
   Enable LaTeX preview and export in Org-mode.
   - Install and configure cdlatex for LaTeX editing
   - Enable org-preview-latex fragment
   - Configure LaTeX export backend
   - Add test for LaTeX preview

** DONE Research org-roam
   :PROPERTIES:
   :Effort:   2
   :Category: :lang:
   :Sprint:   8
   :END:
   Research org-roam for note-taking in Emacs. What is it? How does it work? Is it useful for this config?
   - What: Zettelkasten-style knowledge management over .org files with SQLite database
   - How: Indexes .org files, uses ID-based links, provides graph visualization
   - Verdict: Won't work for this use case - need Mermaid integration and semantic search instead

** TODO Org-mode Mermaid integration
   :PROPERTIES:
   :Effort:   3
   :Category: :lang:
   :Sprint:   9
   :END:
   Enable Mermaid diagram visualization in Org-mode for knowledge management.
   - Configure org-mermaid for code block rendering
   - Enable Mermaid preview in org buffers
   - Add org-babel support for mermaid code blocks
   - Add test for Mermaid org integration

** TODO Semantic search for notes
   :PROPERTIES:
   :Effort:   5
   :Category: :ai:
   :END:
   Implement semantic search across org notes for knowledge discovery.
   - Research embedding-based search approaches
   - Implement vector similarity search
   - Add search indexing for org files
   - Create search UI/interface
   - Add test for semantic search

** TODO Custom Agent Environment
   :PROPERTIES:
   :Effort:   5
   :Category: :ai:
   :END:
   Build low-level AI agent control with url.el + json.el.
   - Create src/agent/ directory structure
   - Implement agent-request.el - HTTP GET/POST via url.el
   - Implement agent-json.el - JSON parse/encode
   - Add unit tests for request functions
   - Add unit tests for JSON functions
   - Create agent-core.el - main interaction loop
   - Implement response handling and parsing
   - Add agent configuration system
   - Create agent-config.el for settings
   - Add integration test for agent workflow

** TODO Research agentic workflows for SOA agents
   :PROPERTIES:
   :Effort:   3
   :Category: :ai:
   :END:
   Research agentic workflow patterns for emulating SOA agents with full control in the agents module.
   - Research SOA (Service-Oriented Architecture) agent patterns
   - Study agent communication protocols (REST, gRPC, message queues)
   - Research agent lifecycle management (spawn, suspend, resume, terminate)
   - Study agent orchestration patterns (choreography vs orchestration)
   - Research agent state management and persistence
   - Study agent security patterns (authentication, authorization, audit)
   - Research agent monitoring and observability
   - Document findings and recommendations for implementation

** DONE Consider tempel-collection
   :PROPERTIES:
   :Effort:   1
   :Category: :editor:
   :Sprint:   7
   :END:
   Evaluate tempel-collection for additional IDE snippets beyond current 88+.

** DONE Expand WORKFLOWS.org
   :PROPERTIES:
   :Effort:   3
   :Category: :docs:
   :Sprint:   6
   :END:
   Created doc/workflows/ folder with sample files for each workflow.

** TODO Scrum Document Safety Guard
   :PROPERTIES:
   :Effort:   1
   :Category: :process:
   :Sprint:   9
   :END:
   Implement pre-commit hook or validation to prevent unauthorized changes to RULES.md, SCRUM-WORKFLOW.md, and PROMPT.md without explicit approval.
   - Create scripts/validate_process_files.sh
   - Add git diff check for RULES.md, SCRUM-WORKFLOW.md, PROMPT.md
   - Return error if unauthorized changes detected
   - Create test case in test/test_document_guard.sh
   - Document usage in doc/ or README

** TODO Phase-specific commands and next steps rules
   :PROPERTIES:
   :Effort:   2
   :Category: :process:
   :END:
   Define which commands and next steps are appropriate in each Scrum phase.
   - Document allowed commands per phase (backlog, planning-poker, sprint-planning, in-progress, review, retrospective)
   - Define valid phase transitions
   - Document what file modifications are allowed per phase
   - Add validation rules for phase-appropriate actions
   - Create reference table in SCRUM-WORKFLOW.md or RULES.md

** TODO Minimize context window per sprint
   :PROPERTIES:
   :Effort:   1
   :Category: :process:
   :END:
   Rule to minimize context window usage during sprints.
   - Read only necessary files per task (not entire codebase)
   - Use targeted grep/search instead of full file reads
   - Commit and log state frequently to enable fresh starts
   - Keep prompts simple and atomic for 32B models
   - Document context minimization guidelines

** TODO Review sprint documentation
   :PROPERTIES:
   :Effort:   2
   :Category: :docs:
   :END:
   Audit sprint docs for true append-only compliance, reformatting only, no text changes, canonical files.
   - Audit SPRINT.org - verify append-only entries
   - Audit BACKLOG.org - verify status changes only
   - Audit SESSION.org - verify structure compliance
   - Check for duplicate rules across files
   - Fix any violations found
   - Document audit results

** DONE More Modes (markdown, yaml, json)
   :PROPERTIES:
   :Effort:   2
   :Category: :lang:
   :Sprint:   3
   :END:
   markdown-mode, yaml-mode, json-mode for config files and docs.

** DONE PDF Tools (pdf-tools)
   :PROPERTIES:
   :Effort:   3
   :Category: :media:
   :Sprint:   4
   :END:
   View and annotate PDFs inside Emacs.

** DONE Image Support (image-mode, image-dired)
   :PROPERTIES:
   :Effort:   2
   :Category: :media:
   :Sprint:   4
   :END:
   View images (png, jpg, gif, svg) and create thumbnails.

** DONE Mermaid Mode (mermaid-mode)
   :PROPERTIES:
   :Effort:   2
   :Category: :media:
   :Sprint:   4
   :END:
   Edit and preview Mermaid diagrams in Emacs.

** DONE SVG Support (svg-tag-mode)
   :PROPERTIES:
   :Effort:   2
   :Category: :media:
   :Sprint:   4
   :END:
   Render SVG images inline with svg-tag-mode.

** DONE yasnippet + Better Snippet Strategy
   :PROPERTIES:
   :Effort:   5
   :Category: :editor:
   :Sprint:   5
   :END:
   Replaced by Tempo (sufficient for current needs). Consider Tempel for future.

** DONE apheleia (format-on-save)
   :PROPERTIES:
   :Effort:   3
   :Category: :editor:
   :Sprint:   5
   :END:
   Not needed: whitespace-cleanup + eglot-format cover 80% of use cases.

** DONE tab-bar / persp-mode (workspaces)
   :PROPERTIES:
   :Effort:   8
   :Category: :editor:
   :Sprint:   5
   :END:
   Not needed: user doesn't require tabs/workspace management.

** DONE gptel + Cloud API (LLM-Workflow)
   :PROPERTIES:
   :Effort:   3
   :Category: :ai:
   :Sprint:   5
   :END:
   Replaced by Custom Agent Environment for low-level control.

** DONE Definition of Done (doc/DOD.md)
   :PROPERTIES:
   :Effort:   1
   :Category: :docs:
   :Sprint:   3
   :END:
   Created Definition of Done document with checklist.

* Done

** DONE Initiales Setup + Refactor + Comfort + Quick Wins
   :PROPERTIES:
   :Effort:   8
   :Category: :editor:
   :Sprint:   1+2
   :END:
