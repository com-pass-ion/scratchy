#+TITLE: Product Backlog
#+STARTUP: overview

All planned features, sorted by category.

* Product Backlog

** TODO apheleia (format-on-save)
   :PROPERTIES:
   :Effort:   3
   :Category: :editor:
   :END:
   Format-on-save for consistent code style.

** TODO yasnippet (richer snippets with tabstops)
   :PROPERTIES:
   :Effort:   5
   :Category: :editor:
   :END:
   Richer snippets with tabstops instead of just Tempo.

** TODO tab-bar / persp-mode (workspaces)
   :PROPERTIES:
   :Effort:   8
   :Category: :editor:
   :END:
   Workspace management for parallel projects.

** TODO Git pre-commit hook
   :PROPERTIES:
   :Effort:   2
   :Category: :devops:
   :END:
   Automatic testing before each commit.

** TODO GitHub Actions CI
   :PROPERTIES:
   :Effort:   5
   :Category: :devops:
   :END:
   CI pipeline for automatic testing.

** TODO Coverage report
   :PROPERTIES:
   :Effort:   2
   :Category: :testing:
   :END:
   Track test coverage.

** TODO CHANGELOG.md
   :PROPERTIES:
   :Effort:   1
   :Category: :docs:
   :END:
   Structured change log.

** TODO Tree-sitter (treesit)
   :PROPERTIES:
   :Effort:   3
   :Category: :editor:
   :END:
   Better syntax highlighting via treesit (built-in Emacs 30).

** TODO More Modes (markdown, yaml, json)
   :PROPERTIES:
   :Effort:   2
   :Category: :lang:
   :END:
   markdown-mode, yaml-mode, json-mode for config files and docs.

** TODO More Snippets (Java, Bash, Rust, Go)
   :PROPERTIES:
   :Effort:   2
   :Category: :lang:
   :END:
   Tempo templates for more languages.

** TODO Expand Org-Mode (org-babel, org-roam)
   :PROPERTIES:
   :Effort:   5
   :Category: :lang:
   :END:
   org-babel for Python/C, org-roam for notes.

** TODO Docker (dockerfile-mode, docker.el)
   :PROPERTIES:
   :Effort:   2
   :Category: :devops:
   :END:
   Dockerfile mode and container management.

** TODO gptel + Cloud API (LLM-Workflow)
   :PROPERTIES:
   :Effort:   3
   :Category: :ai:
   :END:
   LLM integration via gptel with OpenAI/Anthropic API.

* Done

** DONE Initiales Setup + Refactor + Comfort + Quick Wins
   :PROPERTIES:
   :Effort:   8
   :Category: :editor:
   :Sprint:   1+2
   :END:
